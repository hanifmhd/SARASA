<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mis.sarasa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PercakapanActivity"
            android:label="Percakapan"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.mis.sarasa.MainActivity" />
        </activity>
        <activity
            android:name=".PerkenalanActivity"
            android:label="Perkenalan"
            android:parentActivityName=".PerkenalanActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.mis.sarasa.MainActivity" />
        </activity>
        <activity android:name=".SalamActivity" />
        <activity android:name=".AngkaActivity" />
        <activity android:name=".TempattinggalActivity" />
        <activity android:name=".WdsActivity" />
        <activity android:name=".TanggalActivity"></activity>
    </application>

</manifest>